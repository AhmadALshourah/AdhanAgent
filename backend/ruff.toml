[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]          # line-length handled by formatter
fixable = ["ALL"]

[format]
quote-style = "double"
indent-style = "space"

[lint.isort]
known-first-party = ["app"]
